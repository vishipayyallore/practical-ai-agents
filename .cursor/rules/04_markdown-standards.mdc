---
description: Markdown structure and formatting standards
globs: "*.md"
---

# Markdown Standards

## Documentation Files

- Use proper heading hierarchy (H1 → H2 → H3); do not skip levels.
- Include code fence language specifications (` ```python `, ` ```bash `, ` ```text `, etc.).
- Use UTF-8 encoding.
- Keep line length reasonable (~120 chars).

## Code Examples

- Always specify language in code fences.
- Include comments explaining complex or non-obvious logic.
- Show complete, runnable examples when possible.

## File References

- Use relative paths for internal references.
- Verify all file references point to existing files.
- Update references when files are moved or renamed.

## Tables

- Use for framework comparisons, metric summaries, and structured data.
- Align columns consistently.

## Blockquotes

- Use (`>`) for key findings, architectural observations, or important callouts.

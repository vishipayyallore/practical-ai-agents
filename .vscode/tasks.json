{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run CI Checks",
			"type": "shell",
			"command": "uv run isort --check-only --diff src/ && uv run black --check --line-length 127 --target-version py312 src/ && uv run flake8 src/ --count --select=E9,F63,F7,F82 --show-source --statistics"
		},
		{
			"label": "Run Markdown Lint",
			"type": "shell",
			"command": "npx --yes markdownlint-cli2 \"README.md\" \"docs/**/*.md\""
		}
	]
}
